'use strict';

angular.module('feedyApp')
    .factory('Answer', function ($resource, DateUtils) {
        return $resource('api/answers/:id', {}, {
            'query': { method: 'GET', isArray: true},
            'get': {
                method: 'GET',
                transformResponse: function (data) {
                    data = angular.fromJson(data);
                    return data;
                }
            },
            'update': { method:'PUT' }
        });
    });
