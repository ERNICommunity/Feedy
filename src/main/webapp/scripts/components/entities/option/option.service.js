'use strict';

angular.module('feedyApp')
    .factory('Option', function ($resource, DateUtils) {
        return $resource('api/options/:id', {}, {
            'query': { method: 'GET', isArray: true},
            'get': {
                method: 'GET',
                transformResponse: function (data) {
                    data = angular.fromJson(data);
                    return data;
                }
            },
            'update': { method:'PUT' }
        });
    });
